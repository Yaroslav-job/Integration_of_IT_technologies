package council

import (
	"fmt"       // импорт пакета для форматированного ввода/вывода
	"math/rand" // импорт пакета для работы с случайными числами
	"time"      // импорт пакета для работы со временем

	"example.com/City/city" // импорт модуля city
)

func init() {
	rand.Seed(time.Now().UnixNano()) // инициализация генератора случайных чисел
}

func TriggerCouncil(c *city.City) {
	fmt.Println("========================================")
	fmt.Println("🗣️ Совет Горожан собрался для обсуждения важных вопросов!")
	fmt.Println("Выберите одно из предложенных решений:")

	options := []string{
		`"🎪 Устроить ярмарку!" → +20 золота, -1 счастье`,
		`"💰 Повысить налоги!" → +30 золота, -2 счастье`,
		`"🔧 Освободить ремесленников от налогов!" → +3 доход за ход, но -10 золота сразу`,
		`"🍞 Продавать лишнюю провизию!" → +15 золота, -5 жителей`,
		`"🏬 Построить новые торговые ряды!" → +5 дохода, -20 золота`,
		`"🌾 Праздник урожая!" → +10 жителей, -10 золота`,
		`"🍻 Субсидировать трактиры!" → +3 счастья, -5 дохода`,
		`"🍲 Бесплатная раздача еды!" → +5 жителей, +1 счастье, -15 золота`,
		`"🏖️ Устроить день отдыха!" → +2 счастья, но доход за ход временно снижен на 20% (3 хода)`,
		`"🕒 Сократить рабочий день!" → +2 счастья, но -2 дохода`,
		`"⚔️ Военный призыв!" → +3 воина, -5 жителей`,
		`"🛡️ Строительство укреплений!" → -10% к шансу нападения варваров, -30 золота`,
		`"🏋️ Провести военные учения!" → +2 к армии, но -10 золота`,
		`"🔪 Создать ополчение!" → +2 к армии, но -2 счастье`,
		`"🗡️ Купить оружие у торговцев!" → Армия сильнее (на 10%), но -25 золота`,
		`"⛪ Построить часовню!" → +1 счастье, -15 золота`,
		`"🚫 Запретить языческие ритуалы!" → +5 золота, -3 счастье`,
		`"🙏 Устроить молебен за урожай!" → 50% шанс хорошего урожая в следующем ходу`,
		`"💒 Пожертвование храмам!" → +3 счастья, -20 золота`,
		`"👨‍🌾 Назначить старосту!" → +2 к росту жителей, но -2 дохода`,
		`"⚖️ Судить казнокрада!" → +15 золота, но -1 счастье`,
		`"👑 Дать дворянству больше прав!" → +5 золота за ход, но -3 счастье`,
		`"🏛️ Разрешить самоуправление!" → +3 счастья, но временное снижение дохода на 10% (3 хода)`,
		`"🌳 Посадить парк!" → +3 счастья, -20 золота`,
		`"🚰 Построить водоканал!" → Снижает риск эпидемий на 10%, но -30 золота`,
		`"🌲 Разрешить вырубку лесов!" → +10 золота, но +5% шанс наводнения`,
		`"🦌 Ограничить охоту!" → +1 счастье, но -5 жителей`,
	}

	selectedOptions := make([]string, 0, 3) // создание среза для выбранных опций
	usedIndices := make(map[int]bool)       // создание карты для отслеживания использованных индексов
	for len(selectedOptions) < 3 {
		index := rand.Intn(len(options)) // случайный выбор опции
		if !usedIndices[index] {
			selectedOptions = append(selectedOptions, options[index]) // добавление опции в выбранные
			usedIndices[index] = true                                 // отметка индекса как использованного
		}
	}

	for i, option := range selectedOptions {
		fmt.Printf("%d. %s\n", i+1, option)
	}

	fmt.Print("Введите номер решения (1-3): ")
	var choice int
	fmt.Scan(&choice) // считывание выбора пользователя

	if choice < 1 || choice > 3 {
		fmt.Println("❌ Неверный выбор, попробуйте снова. ❌")
		return
	}

	decision := selectedOptions[choice-1]
	if !canAffordDecision(c, decision) {
		fmt.Println("❌ Недостаточно золота для принятия этого решения. ❌")
		return
	}

	applyDecision(c, decision) // применение выбранного решения

	// Обновление показателей после принятия решения
	c.UpdateIncome()
	c.UpdateLevel()
	c.CheckCityLevel()
	c.UpdateArmy()
	c.UpdatePopulationGrowth()
	c.UpdateDefenseChance() // обновление шанса нападения варваров
	c.UpdateEpidemicRisk()  // обновление риска эпидемий
	c.UpdateFloodRisk()     // обновление риска наводнения

	fmt.Println("Решение принято:", decision)
}

func canAffordDecision(c *city.City, decision string) bool {
	switch decision {
	case `"🔧 Освободить ремесленников от налогов!" → +3 доход за ход, но -10 золота сразу`:
		return c.Treasury >= 10
	case `"🏬 Построить новые торговые ряды!" → +5 дохода, -20 золота`:
		return c.Treasury >= 20
	case `"🌾 Праздник урожая!" → +10 жителей, -10 золота`:
		return c.Treasury >= 10
	case `"🍲 Бесплатная раздача еды!" → +5 жителей, +1 счастье, -15 золота`:
		return c.Treasury >= 15
	case `"🛡️ Строительство укреплений!" → -10% к шансу нападения варваров, -30 золота`:
		return c.Treasury >= 30
	case `"🏋️ Провести военные учения!" → +2 к армии, но -10 золота`:
		return c.Treasury >= 10
	case `"🗡️ Купить оружие у торговцев!" → Армия сильнее (на 10%), но -25 золота`:
		return c.Treasury >= 25
	case `"⛪ Построить часовню!" → +1 счастье, -15 золота`:
		return c.Treasury >= 15
	case `"💒 Пожертвование храмам!" → +3 счастья, -20 золота`:
		return c.Treasury >= 20
	case `"🌳 Посадить парк!" → +3 счастья, -20 золота`:
		return c.Treasury >= 20
	case `"🚰 Построить водоканал!" → Снижает риск эпидемий на 10%, но -30 золота`:
		return c.Treasury >= 30
	default:
		return true
	}
}

func applyDecision(c *city.City, decision string) {
	switch decision {
	case `"🎪 Устроить ярмарку!" → +20 золота, -1 счастье`:
		c.Treasury += 20
		c.Happiness -= 1
	case `"💰 Повысить налоги!" → +30 золота, -2 счастье`:
		c.Treasury += 30
		c.Happiness -= 2
	case `"🔧 Освободить ремесленников от налогов!" → +3 доход за ход, но -10 золота сразу`:
		c.Treasury -= 10
		c.Income += 3
	case `"🍞 Продавать лишнюю провизию!" → +15 золота, -5 жителей`:
		c.Treasury += 15
		c.Residents -= 5
	case `"🏬 Построить новые торговые ряды!" → +5 дохода, -20 золота`:
		c.Treasury -= 20
		c.Income += 5
	case `"🌾 Праздник урожая!" → +10 жителей, -10 золота`:
		c.Treasury -= 10
		c.Residents += 10
	case `"🍻 Субсидировать трактиры!" → +3 счастья, -5 дохода`:
		c.Happiness += 3
		c.Income -= 5
	case `"🍲 Бесплатная раздача еды!" → +5 жителей, +1 счастье, -15 золота`:
		c.Treasury -= 15
		c.Residents += 5
		c.Happiness += 1
	case `"🏖️ Устроить день отдыха!" → +2 счастья, но доход за ход временно снижен на 20% (3 хода)`:
		c.Happiness += 2
		c.TemporaryIncomeReduction = 3 // временное снижение дохода на 20% на 3 хода
	case `"🕒 Сократить рабочий день!" → +2 счастья, но -2 дохода`:
		c.Happiness += 2
		c.Income -= 2
	case `"⚔️ Военный призыв!" → +3 воина, -5 жителей`:
		c.Army += 3
		c.Residents -= 5
	case `"🛡️ Строительство укреплений!" → -10% к шансу нападения варваров, -30 золота`:
		c.Treasury -= 30
		c.DefenseChance *= 0.9 // уменьшение шанса нападения варваров на 10%
	case `"🏋️ Провести военные учения!" → +2 к армии, но -10 золота`:
		c.Treasury -= 10
		c.Army += 2
	case `"🔪 Создать ополчение!" → +2 к армии, но -2 счастье`:
		c.Army += 2
		c.Happiness -= 2
	case `"🗡️ Купить оружие у торговцев!" → Армия сильнее (на 10%), но -25 золота`:
		c.Treasury -= 25
		c.ArmyStrengthBonus = 1.1 // увеличение силы армии на 10%
	case `"⛪ Построить часовню!" → +1 счастье, -15 золота`:
		c.Treasury -= 15
		c.Happiness += 1
	case `"🚫 Запретить языческие ритуалы!" → +5 золота, -3 счастье`:
		c.Treasury += 5
		c.Happiness -= 3
	case `"🙏 Устроить молебен за урожай!" → 50% шанс хорошего урожая в следующем ходу`:
		if rand.Float64() < 0.5 {
			fmt.Println("Молебен был успешен! Ожидается хороший урожай в следующем ходу.")
			c.HarvestBonus = true // добавление логической переменной для бонуса урожая
		} else {
			fmt.Println("Молебен не принес ожидаемых результатов.")
		}
	case `"💒 Пожертвование храмам!" → +3 счастья, -20 золота`:
		c.Treasury -= 20
		c.Happiness += 3
	case `"👨‍🌾 Назначить старосту!" → +2 к росту жителей, но -2 дохода`:
		c.PopulationGrowth += 2
		c.Income -= 2
		c.UpdatePopulationGrowth() // обновление роста населения
	case `"⚖️ Судить казнокрада!" → +15 золота, но -1 счастье`:
		c.Treasury += 15
		c.Happiness -= 1
	case `"👑 Дать дворянству больше прав!" → +5 золота за ход, но -3 счастье`:
		c.Income += 5
		c.Happiness -= 3
	case `"🏛️ Разрешить самоуправление!" → +3 счастья, но временное снижение дохода на 10% (3 хода)`:
		c.Happiness += 3
		c.TemporaryIncomeReduction = 3 // временное снижение дохода на 10% на 3 хода
	case `"🌳 Посадить парк!" → +3 счастья, -20 золота`:
		c.Treasury -= 20
		c.Happiness += 3
	case `"🚰 Построить водоканал!" → Снижает риск эпидемий на 10%, но -30 золота`:
		c.Treasury -= 30
		c.EpidemicRiskReduction = 0.9 // уменьшение риска эпидемий на 10%
	case `"🌲 Разрешить вырубку лесов!" → +10 золота, но +5% шанс наводнения`:
		c.Treasury += 10
		c.FloodRiskIncrease = 1.05 // увеличение риска наводнения на 5%
	case `"🦌 Ограничить охоту!" → +1 счастье, но -5 жителей`:
		c.Happiness += 1
		c.Residents -= 5
	}

	// Отладочные сообщения для проверки изменений
	fmt.Printf("Казна: %d золота\n", c.Treasury)
	fmt.Printf("Счастье: %d/10\n", c.Happiness)
	fmt.Printf("Доход: %.2f/ход\n", c.Income)
	fmt.Printf("Армия: %d воинов\n", c.Army)
	fmt.Printf("Жители: %d\n", int(c.Residents))

	// Обновление показателей после принятия решения
	c.UpdateIncome()
	c.UpdateLevel()
	c.CheckCityLevel()
	c.UpdateArmy()
	c.UpdatePopulationGrowth() // обновление роста населения
	c.UpdateDefenseChance()    // обновление шанса нападения варваров
	c.UpdateEpidemicRisk()     // обновление риска эпидемий
	c.UpdateFloodRisk()        // обновление риска наводнения

	fmt.Println("Решение принято:", decision)
}
